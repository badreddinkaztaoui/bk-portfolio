import type { NextPage } from "next";
import Layout from "src/layout";
import styled from "styled-components";

const Home: NextPage = () => {
  return (
    <div>
      <Layout />
    </div>
  );
};

export default Home;
